// Missions.js
import React from 'react';
import '../App.css'; // Import CSS file for shared styles

const Missions = () => {
  return (
    <div className="App-main shared-container">
      <h1>Missions</h1>
      <select className="shared-dropdown">
        {/* Options for mission selection */}
      </select>
      {/* Display mission data */}
      {/* Add your mission data display here */}
    </div>
  );
};

export default Missions;
